// Here all types related to the configuration of the app
// Currently not used at all
// If the project was to be done professionally,
// the configuration variables should be typed as well

namespace NodeJS {
  interface ProcessEnv {
    PORT: string;
    DB_URI: string;
  }
}
